# Non-secret local reference profile.
APP_ENV=development
AUTH_MODE=none
SECRET_SOURCE=local-environment
AUDIT_SINK=stdout
TELEMETRY_EXPORTER=console
PORT=3000
REQUEST_TIMEOUT_MS=30000
MAX_CONCURRENT_REQUESTS=20
RATE_LIMIT_PER_MINUTE=60
MODEL_MAX_ATTEMPTS=1
MODEL_MAX_OUTPUT_TOKENS=700
MONTHLY_MODEL_BUDGET_USD=0
MIN_REPLICAS=1
MULTI_ZONE=false
AUTOSCALING=false
CARBON_AWARE_REGION=false
INFRASTRUCTURE_AS_CODE=false
AUTOMATIC_ROLLBACK=false
BACKUP_RESTORE_TESTED=false
DATA_RETENTION_DAYS=7

# MCP connector layer. Off in every reference profile: enabling an egress path is an owner
# decision with a threat model behind it, not a default. See docs/MCP_CONNECTORS.md.
MCP_CONNECTORS_ENABLED=false
MCP_CREDENTIAL_MODE=denied
