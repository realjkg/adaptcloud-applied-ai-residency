# Agent instructions

Follow `CLAUDE.md` and `.cursor/rules/adapt-cloud.mdc`. Keep changes bounded to the assigned issue. Never weaken a control to make a test pass. Preserve deterministic behavior when no model key is configured. Use pull requests and include evidence in the PR body.
